[pytest]
markers =
    acid: Iceberg ACID audit concurrency tests
    integration: End-to-end integration tests
    generator: Event generator tests
    kafka: Kafka streaming tests
    iceberg: Apache Iceberg catalog and table tests
    minio: MinIO object storage tests
    schema: Schema registry and validation tests
    streaming: Flink and end-to-end stream tests
